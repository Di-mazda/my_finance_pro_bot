{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "playwright": {
      "inputs": [ { "step": "install" } ],
      "commands": [ "python -m playwright install --with-deps chromium" ]
    }
  },
  "deploy": {
    "inputs": [
      "...",
      { "step": "playwright" }
    ]
  }
}